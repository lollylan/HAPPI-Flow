{
  "name": "@haeppi/shared",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Domaenentypen, Zeit-/Stundenrechnung und der Dienstplan-Algorithmus. Ohne Datenbankzugriff, damit vollstaendig testbar.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  }
}
